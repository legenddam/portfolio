<a class="secondary button" href="/admin">Go Back</a>
<h3>Edit Proejct</h3>
<form method = 'post' action='/admin/edit/{{project.id}}' enctype="multipart/form-data">
    <div class='form-gruop'>
        <label>Title</label>
        <input type="text", class="form-control", name="title" placeholder="Title" value="{{project.title}}">
    </div>
    <div class='form-group'>
        <label>Service</label>
        <input type="text", class="form-control", name="service" placeholder="Service" value="{{project.service}}">
    </div>
    <div class='form-gruop'>
        <label>Client</label>
        <input type="text", class="form-control", name="client" placeholder="Client" value="{{project.client}}">
    </div>

    <div class='form-group'>
        <label>Description</label>
        <textarea class="form-control", name="description">{{project.description}}</textarea>

    </div>
 
    <div class='form-gruop'>
        <label>ProjectURL</label>
        <input type="text", class="form-control", name="projecturl" placeholder="ProjectURL" value="{{project.url}}">
    </div>
    
    <div class='form-gruop'>
        <label>ProjectDate</label>
        <input type="date", class="form-control", name="projectdate" value="{{project.date}}">
    </div>
    <div class='form-group'>
        <label>Image</label>
        <input type="file", class="form-control", name="projectimage" value="{{project.image}}">
    </div>
    <input type="submit" class="form-control" value="Submit">
</form>